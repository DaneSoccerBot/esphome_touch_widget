substitutions:
  # Zentrale Versions-Referenz fuer das Projekt.
  # Diese Werte muessen in display48_device_base.yaml und
  # simulator_device_base.yaml gespiegelt werden, da ESPHome
  # !include innerhalb von GitHub-Imports nicht aufloest.
  # Primaerquelle: build_settings.yaml (Root)
  project_name: esphome.touch_widget
  project_version: "0.2.0-dev"

  # Kann pro User-YAML auf Tag/Commit gesetzt werden, z.B. v0.1.1 oder 7cf56a2
  package_ref: main
  tile_dashboard_components_source: github://DaneSoccerBot/esphome_touch_widget/components@${package_ref}
