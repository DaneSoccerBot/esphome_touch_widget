esphome:
  name: ${device_name}
  friendly_name: ${friendly_name}
  project:
    name: ${project_name}
    version: ${project_version}
  platformio_options:
    build_flags:
      - "-D_USE_MATH_DEFINES"
      - "-fpermissive"

host:

logger:
  level: ${logger_level}

api:
